services:
  corvustunnel:
    build: .
    container_name: corvustunnel
    ports:
      - "8000:8000"
      - "8001:8001"
    volumes:
      - ./projects:/workspace
      - corvus_home:/home/corvus   # persists agent auth tokens across restarts
    restart: unless-stopped

volumes:
  corvus_home:
